<p id="notice"><%= notice %></p>

<section class="section">
  <div class="container">
    <div class="columns">
      <% if user_signed_in? %>
        <%= render 'profile' %>
        <% end %>
      <%= render 'feed' %>
      <%= render 'who-to-collab' %>
    </div>
  </div>
</section>

