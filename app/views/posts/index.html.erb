<p id="notice"><%= notice %></p>

<section class="section">
  <div class="container">
    <div class="columns">
      <%= render 'feed' %>
      <%= render 'who-to-collab' %>
    </div>
  </div>
</section>

